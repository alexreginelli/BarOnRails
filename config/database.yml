default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: bar_inventory_development
  username: postgres
  password: password
  host: localhost
  port: 5433

test:
  <<: *default
  database: bar_inventory_test
  username: postgres
  password: password
  host: localhost
  port: 5433

production:
  url: <%= ENV['DATABASE_URL'] %>